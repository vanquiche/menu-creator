import { FilterMenuPipe } from './filter-menu.pipe';

describe('FilterMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
